Frontend placeholder

We will scaffold a React app here. For now the backend serves APIs. To create the frontend:

# on your machine
cd frontend
npx create-react-app .

Build and place files into frontend/build for nginx to serve.
